package model;

import java.io.Serializable;

/**
 * The Enum Color representing the colors that a family member can have
 */
public enum Color implements Serializable {
    ORANGE,
    BLACK,
    WHITE,
    NEUTRAL,
}
